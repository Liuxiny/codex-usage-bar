# Codex Usage Bar v0.6.3

## 变化

- 删除收缩态日期文字遗留的向下 1px 偏移，使圆环、百分比和日期使用同一垂直中轴。
- 昨日 Token 改为按 Codex App Server 的 UTC 日界线读取，不再受 Windows 本地时区日期影响。
- 官方昨日没有日桶时显示 0，避免跨日后继续显示旧数据。
- 收缩态和展开态内部统一使用 Bold 字重，通过文字颜色区分信息层级。
- 展开态 Token 数值与 K/M/B 缩写使用强调色；标签、分隔符及 `Token` 字样使用日期文字颜色。
- 保留 0.6.2 的清晰原生字体绘制、18px 圆环、动态展开间距和展开态 1px 窗口偏移。

## 验证

- .NET Framework 编译及内置自测通过。
- 自测覆盖 UTC 跨本地日期边界、昨日无用量归零、18px 圆环、字体配置和展开态定位。
- 0.6.2 可原位覆盖升级。
