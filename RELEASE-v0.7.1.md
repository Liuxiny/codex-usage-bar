# v0.7.1

修复独立运行时的 HTTPS SecureChannelFailure，并改善 CC Switch 数据目录选择。

- 复现：旧版 EXE 默认 SSL3/TLS 1.0，真实站点拒绝握手；PowerShell 托管测试未覆盖该运行环境。
- 查询显式启用 TLS 1.2，保留系统证书校验；独立进程真实查询返回 3 行。
- 打开设置时自动查找并填入数据目录，新增“自动查找”和“浏览”按钮。
- 明确提示选择包含 cc-switch.db 的文件夹；错误目录在保存/查询前提示，不悄悄回退成官方用量。
- 支持粘贴带引号的目录和 cc-switch.db 文件路径；本地化目录与 TLS 错误提示。
- 新增旧协议默认值、证书校验保持、目录查找/错误目录回归；提供独立进程诊断源码 installer/tests/usage-process-probe.cs。

## 设置和托盘界面

- 设置窗口使用悬浮窗同源主题色与字体，深浅模式同步；参数、脚本、查询结果分区展示。
- CC Switch 模式展示目录及连接说明，独立模式展示凭据和脚本编辑区。
- 托盘菜单统一背景、圆角、行距、悬停和勾选样式，包括子菜单；避免长菜单文字截断。
- 保持正常额度悬浮窗样式不变；仅将查询中、失败等无额度状态文字居中。
- 深浅主题切换与重复应用主题渲染通过，修复 WinForms 相等字体实例缓存导致的失效字体引用。
