# Codex Usage Bar v0.7.5 — 支持 CC Switch 与第三方 API

本版本新增 CC Switch 用量查询兼容，并支持在未安装 CC Switch 时独立配置其他第三方 API。官方 Codex 用量查询仍可选用。

## 数据源

- 自动读取 CC Switch 当前 Codex 服务商及其自定义 `request + extractor` 查询脚本。
- 按当前 Windows 用户的数据目录规则查找 `.cc-switch`，支持手动选择目录及配置提示。
- 托盘“数据源”提供自动、官方、CC Switch 和独立配置模式。
- 独立配置可维护接口地址、访问参数及提取器脚本，附带测试查询功能；其他第三方 API 需要提供可访问的用量接口及匹配的提取器。
- 兼容套餐额度、重置时间、站内余额和有效的余额折合周额度；第三方数据与官方用量分别展示。

## 界面

- 收缩显示圆环、百分比、重置时间及 `$ 数值 USD`；展开显示条形进度、独立余额栏及可用的估算栏。
- 百分比按实际空间与时分居中对齐；空间不足时放在额度标签右侧，不按5小时或周额度区分。
- 余额使用单空格间距，数值加粗并使用强调色，$ 和 USD 使用常规字重；提示文字居中。
- 悬浮窗适配 Windows DPI，收缩为16逻辑像素文字，展开遵循外观字号；设置窗口与托盘样式同步优化。

## 安装与验证

下载并运行 `CodexUsageBar-Setup-v0.7.5.exe`。

原生自测、CC Switch 提取器兼容性测试及100%、125%、150%、200%模拟DPI渲染已通过；实际跨显示器切换尚未实测。本次发布直接上传已有安装包，未重新编译。

SHA256：`FEA7BC497C4C33A43201B541F7466078BD51CE77E1320415F00B2B7B9BDE2541`
