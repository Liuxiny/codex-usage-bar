# v0.7.2

第三方用量悬浮窗改为与官方一致的圆环/条形额度布局。

- 收缩：额度圆环、重置时间，末尾显示 `$金额 单位`；无额度提示整体居中。
- 展开：条形额度、重置时间、独立余额栏，估算显示在余额下一栏。估算不可用时隐藏整栏及分隔线，0 仍显示。
- 已知套餐响应补充原始重置时间，保留提取器数值和有效状态；不把估算当真实额度。
- 收缩字号 16px，展开跟随 UI 字号；标签、说明、货币符号与单位不加粗。
- 托盘入口改为“数据源”，菜单文字垂直居中；选项简化为“独立配置”。

验证：PRO 单额度、双额度、过期窗口、估算缺失和估算0、字体大小、深浅设置与菜单渲染，以及原有官方自检。
