# v0.7.0

支持直接执行 CC Switch 保存的自定义用量脚本，并提供不依赖 CC Switch 的独立查询设置。

- 自动读取当前 Codex 服务商及专用用量凭据；只读数据库，配置切换后刷新。
- 独立维护 Base URL、API Key、Access Token、User ID、JavaScript、超时与刷新间隔；Windows DPAPI 加密保存。
- 多行展示余额、套餐额度、估算和无效原因，保持来源独立。
- 查询失败保留状态；重新执行缓存提取器判断过期与倒计时，禁止显示旧服务商的在途结果。
- 内置用户提供的套餐查询模板，可在设置窗口直接测试。

验证：原有自检、新增 JS/SQLite/DPAPI/本地 HTTP 回归和窗口渲染通过；使用本机 DespAI 保存的脚本完成真实查询，返回 3 行、2 行有效。

升级后默认自动跟随 CC Switch；没有 CC Switch 时继续显示官方用量。独立配置入口位于托盘“用量数据源设置”。
